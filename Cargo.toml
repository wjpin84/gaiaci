
[package]
name = "gaiaci"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.3", features = ["derive"] }
mlua = { version = "0.10.5", features = ["lua54", "vendored", "serialize", "async"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "time"] }

[dev-dependencies]
assert_cmd = "2.0"
tempfile = "3.10"
predicates = "3.1"